// Legacy views module — kept for backward compatibility.
// All rendering has moved to tui::workspaces and tui::chrome.
pub mod backtest_detail;
pub mod backtest_form;
pub mod backtest_list;
pub mod node_status;
pub mod strategy_list;
